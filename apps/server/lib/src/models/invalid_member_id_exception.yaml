### Serialized to the app client as a 400 so `removeMember` rejects a
### non-numeric member id with a typed validation failure instead of a generic
### 500 (FormatException). Serverpod serializes any `SerializableException`
### thrown from an endpoint as an HTTP 400 response (see
### serverpod `_reportException`), and the generated client rethrows this type.
exception: InvalidMemberIdException
fields: {}