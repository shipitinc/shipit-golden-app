server:
  # 8081 is the Flutter web dev port (melos run dev:app); keep the test server
  # off it so a local test boot can never collide with the app under dev.
  port: 8082
  host: localhost

database:
  host: localhost
  port: 5432
  name: shipit_golden_test
  user: shipit
  # password is in passwords.yaml (test section)
  maxConnectionCount: 4