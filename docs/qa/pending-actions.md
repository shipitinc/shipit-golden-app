# Pending Action Items

Open items tracked outside of issue trackers (not blockers; captured so they
are not lost between sessions).

| # | Item | Owning gate | Status |
|---|------|-------------|--------|
| 1 | Golden check runs with the real bundled Inter TTFs; candidate baselines were regenerated on the Linux CI host via the manual `.github/workflows/goldens-update.yml` and promoted to APPROVED by design/human sign-off on 2026-09-08 against `shipit_ui@1207004`, then re-approved on 2026-09-09 against `shipit_ui@d6abf9a` (token-tree refactor). Local macOS runs intentionally show a known ~1% rasterizer diff (CoreText vs FreeType) on exactly the two golden tests — do NOT regenerate on macOS; use the workflow with `design_revision` + `reviewer` inputs. | design/human approval | completed 2026-09-09 |
| 2 | Dev environment: `localhost:8080` is owned by an unrelated `partnerhub-*` server. The golden server runs on `8099` via runtime env overrides (`SERVERPOD_API_SERVER_PORT=8099` + `SERVERPOD_API_SERVER_PUBLIC_*`) and the app via `--dart-define=API_BASE_URL=http://localhost:8099`. No committed port change — do not stop the partnerhub server. | ops note | informational |
| 3 | Form-field validation showcase (login/register + add-member per-field errors) approved by design/human on 2026-09-08; covered by widget tests (`authentication_flow_widget_test.dart`, `add_member_dialog_test.dart`). Manual on-device pass optional for any future visual QA. | QA | completed 2026-09-08 |
| 4 | Dark mode was broken in shipit_ui (`shipitDarkTheme()` used static light surfaces with white text). Documented as UPSTREAM_UI_GAP-011 and reported as shipitinc/shipit-ui#10 (2026-09-08); fixed upstream in the token-tree refactor (`shipit_ui@d6abf9a`). Golden App bumped the pin, migrated all token reads to the `context.*` API (`AppThemeContext`), and added a dark-mode regression test (`test/theme/dark_mode_test.dart`: genuinely dark scaffolds + WCAG AA body-text contrast). No app-side override kept. | shipit-ui upstream | completed 2026-09-09 |
| 5 | Household/programs endpoints were in-memory stubs (`STUB / DEV_PENDING`) with DB tables unused. Now PostgreSQL-backed: `household_endpoint.dart` auto-creates a household per authenticated user and persists members; `programs_endpoint.dart` reads `programs`. Seed data added via `apps/server/bin/seed.dart` (`melos run seed`, idempotent, Serverpod maintenance-role pattern). Simulated 600 ms stub latency removed. | QA | completed 2026-09-09 |
| 6 | UPSTREAM_UI_GAP-012 resolved in `shipit_ui@18d1a5d6` (`AppTextButton` + `AppIconButton`, closes shipitinc/shipit-ui#11). Golden App bumped the pin and replaced every raw Material fallback: `login_screen.dart` mode toggle → `AppTextButton`, back arrow → `AppIconButton`; `household_screen.dart` / `programs_screen.dart` refresh + sign-out app-bar actions → `AppIconButton` (replacing the `IconButton` + `AppTooltip` composition). The approved login baselines (`login_sign_in.png`, `login_register.png`) re-rendered because of the button swaps and were regenerated + re-approved against `shipit_ui@18d1a5d6` via the `.github/workflows/goldens-update.yml` workflow (`design_revision=shipit_ui@18d1a5d6`, reviewer tariq, Linux CI host only, registry update in the same commit; see `goldens_registry.md`). | design/human approval | completed 2026-09-10 |
| 7 | AEF review finding (removeMember 500 on non-numeric member id): `household_endpoint.dart` now resolves `memberId` via `int.tryParse` and throws a serialized `InvalidMemberIdException`, which Serverpod surfaces as an HTTP 400 and the client rethrows as a typed exception mapped to `ValidationFailure` in `error_translator.dart`. New protocol exception was generated and `.generated_manifest.json` re-snapshotted; covered by a DB-backed server test and a translator test. | QA | completed 2026-09-10 |
| 8 | AEF review finding (section-static `SessionExpiredNotifier`): converted from a section-static listener bridge to an injectable instance — `SessionExpiredNotifier.shared` is the production default (mirrors `ServerpodClientProvider.shared`) and `mapAppFailure` accepts a `notifier:` parameter so tests/future DI can supply a dedicated instance (isolated-instance test added). Residual: a router-listen hook or full DI-container inversion is intentionally deferred until the Phase-2 app shell exists; current behavior is unchanged, tested, and documented on the class. | engineering follow-up | completed 2026-09-10 / tracked |
| 9 | AEF review finding (no in-app navigation between `/household` and `/programs`): implemented 2026-09-10 under direct approval. Authenticated screens now share an app shell (`app/shell/app_shell.dart`) via GoRouter `StatefulShellRoute.indexedStack` (each tab keeps its Navigator, preserving BLoC + scroll state) using the approved shipit_ui `AppNavigationRail` with Household / Programs destinations — auto-extends at desktop, collapses with tooltips below (no new Material fallbacks, no mobile bottom-nav invented). Covered by `test/features/navigation/app_shell_widget_test.dart` (landing, switching, desktop-extension, re-select). | design/human approval | completed 2026-09-10 |
| 10 | AEF review finding (local constants lack upstream tokens): `kSmallIconSize` (16), `_labelWeight` (`w600`), and `_authCardMaxWidth` (440). Reported upstream 2026-09-10 as shipitinc/shipit-ui#12, #13 and #14; all three shipped in `shipit_ui@526926d` (`icon.size.sm`, `font.weight.semibold`, `layout.maxWidth.form`). Golden App bumped the pin and consumed the tokens (`program_card.dart`, `household_header.dart`, `login_screen.dart`), deleting the constants and `core/shared/ui_constants.dart`; token values are identical to the replaced constants (16 / w600 / 440) so rendering — including the APPROVED login baselines — is unchanged. | shipit-ui upstream | completed 2026-09-10 |