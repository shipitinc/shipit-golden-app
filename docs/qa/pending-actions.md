# Pending Action Items

Open items tracked outside of issue trackers (not blockers; captured so they
are not lost between sessions).

| # | Item | Owning gate | Status |
|---|------|-------------|--------|
| 1 | Golden check runs with the real bundled Inter TTFs; candidate baselines were regenerated on the Linux CI host via the manual `.github/workflows/goldens-update.yml` and promoted to APPROVED by design/human sign-off on 2026-09-08 against `shipit_ui@1207004`, then re-approved on 2026-09-09 against `shipit_ui@d6abf9a` (token-tree refactor). Local macOS runs intentionally show a known ~1% rasterizer diff (CoreText vs FreeType) on exactly the two golden tests — do NOT regenerate on macOS; use the workflow with `design_revision` + `reviewer` inputs. | design/human approval | completed 2026-09-09 |
| 2 | Dev environment: `localhost:8080` is owned by an unrelated `partnerhub-*` server. The golden server runs on `8099` via runtime env overrides (`SERVERPOD_API_SERVER_PORT=8099` + `SERVERPOD_API_SERVER_PUBLIC_*`) and the app via `--dart-define=API_BASE_URL=http://localhost:8099`. No committed port change — do not stop the partnerhub server. | ops note | informational |
| 3 | Form-field validation showcase (login/register + add-member per-field errors) approved by design/human on 2026-09-08; covered by widget tests (`authentication_flow_widget_test.dart`, `add_member_dialog_test.dart`). Manual on-device pass optional for any future visual QA. | QA | completed 2026-09-08 |
| 4 | Dark mode was broken in shipit_ui (`shipitDarkTheme()` used static light surfaces with white text). Documented as UPSTREAM_UI_GAP-011 and reported as shipitinc/shipit-ui#10 (2026-09-08); fixed upstream in the token-tree refactor (`shipit_ui@d6abf9a`). Golden App bumped the pin, migrated all token reads to the `context.*` API (`AppThemeContext`), and added a dark-mode regression test (`test/theme/dark_mode_test.dart`: genuinely dark scaffolds + WCAG AA body-text contrast). No app-side override kept. | shipit-ui upstream | completed 2026-09-09 |
| 5 | Household/programs endpoints were in-memory stubs (`STUB / DEV_PENDING`) with DB tables unused. Now PostgreSQL-backed: `household_endpoint.dart` auto-creates a household per authenticated user and persists members; `programs_endpoint.dart` reads `programs`. Seed data added via `apps/server/bin/seed.dart` (`melos run seed`, idempotent, Serverpod maintenance-role pattern). Simulated 600 ms stub latency removed. | QA | completed 2026-09-09 |
| 6 | UPSTREAM_UI_GAP-012 resolved in `shipit_ui@18d1a5d6` (`AppTextButton` + `AppIconButton`, closes shipitinc/shipit-ui#11). Golden App bumped the pin and replaced every raw Material fallback: `login_screen.dart` mode toggle → `AppTextButton`, back arrow → `AppIconButton`; `household_screen.dart` / `programs_screen.dart` refresh + sign-out app-bar actions → `AppIconButton` (replacing the `IconButton` + `AppTooltip` composition). The approved login baselines (`login_sign_in.png`, `login_register.png`) re-rendered because of the button swaps and were regenerated + re-approved against `shipit_ui@18d1a5d6` via the `.github/workflows/goldens-update.yml` workflow (`design_revision=shipit_ui@18d1a5d6`, reviewer tariq, Linux CI host only, registry update in the same commit; see `goldens_registry.md`). | design/human approval | completed 2026-09-10 |
| 7 | AEF review finding (removeMember 500 on non-numeric member id): `household_endpoint.dart` now resolves `memberId` via `int.tryParse` and throws a serialized `InvalidMemberIdException`, which Serverpod surfaces as an HTTP 400 and the client rethrows as a typed exception mapped to `ValidationFailure` in `error_translator.dart`. New protocol exception was generated and `.generated_manifest.json` re-snapshotted; covered by a DB-backed server test and a translator test. | QA | completed 2026-09-10 |
| 8 | AEF review finding (section-static `SessionExpiredNotifier`): converted from a section-static listener bridge to an injectable instance — `SessionExpiredNotifier.shared` is the production default (mirrors `ServerpodClientProvider.shared`) and `mapAppFailure` accepts a `notifier:` parameter so tests/future DI can supply a dedicated instance (isolated-instance test added). Residual resolved 2026-09-12: the listener now lives as a router-listen hook — `AppRouter` is an instance that owns the `GoRouter`, subscribes to the notifier in its constructor, dispatches `AuthenticationEvent.sessionExpired()` to the auth bloc, and unsubscribes + disposes the router in `dispose()` (no widget in the tree observes the notifier; `_AppView` just gives the router a lifecycle). Covered by a shell widget test that announces `session_expired` and asserts the app routes back to login. | engineering follow-up | completed 2026-09-10 / residual completed 2026-09-12 |
| 9 | AEF review finding (no in-app navigation between `/household` and `/programs`): implemented 2026-09-10 under direct approval. Authenticated screens now share an app shell (`app/shell/app_shell.dart`) via GoRouter `StatefulShellRoute.indexedStack` (each tab keeps its Navigator, preserving BLoC + scroll state) using the approved shipit_ui `AppNavigationRail` with Household / Programs destinations — auto-extends at desktop, collapses with tooltips below (no new Material fallbacks, no mobile bottom-nav invented). Covered by `test/features/navigation/app_shell_widget_test.dart` (landing, switching, desktop-extension, re-select). | design/human approval | completed 2026-09-10 |
| 10 | AEF review finding (local constants lack upstream tokens): `kSmallIconSize` (16), `_labelWeight` (`w600`), and `_authCardMaxWidth` (440). Reported upstream 2026-09-10 as shipitinc/shipit-ui#12, #13 and #14; all three shipped in `shipit_ui@526926d` (`icon.size.sm`, `font.weight.semibold`, `layout.maxWidth.form`). Golden App bumped the pin and consumed the tokens (`program_card.dart`, `household_header.dart`, `login_screen.dart`), deleting the constants and `core/shared/ui_constants.dart`; token values are identical to the replaced constants (16 / w600 / 440) so rendering — including the APPROVED login baselines — is unchanged. | shipit-ui upstream | completed 2026-09-10 |
| 11 | Program detail + membership flow (Phase-2 golden path): `program_members` model (unique programId+householdId) with migration `20260911025700630`; `ProgramsEndpoint.getById/joinProgram/getMembership/cancelMembership`; shared `HouseholdService` (household auto-create extracted from `household_endpoint`); join/cancel are household-scoped and idempotent, and an unknown program id throws a serializable `ProgramNotFoundException` (400) mapped to `ValidationFailure` in `error_translator.dart`. App: `ProgramDetailsBloc` (load program + membership, join/cancel with `isMutating` button feedback per loading-states doc), `/programs/:id` route, `ProgramDetailsScreen` (DESIGN_PENDING layout/CTA — shipit_ui components only) and wired `View Details` navigation. Follow-up: capture a DESIGN_PENDING golden baseline for the detail screen on the Linux CI host via `.github/workflows/goldens-update.yml` once design approves layout/CTA copy. | design/human approval | completed 2026-09-10 / tracked |
| 12 | `program_members.householdId` is `String` while `programMembers.programId` is `int`, and `household_members.householdId` is also `String` — all three mismatch the actual column type (`bigserial`/`bigint`) of `households.id`. The mix was not caught at initial scaffold and both endpoints and seed code call `household.id.toString()` to bridge the gap. Aligned 2026-09-12: changed `program_member.yaml` and `household_member.yaml` `householdId` fields from `String` to `int`, removed the `toString()` conversions in endpoints/seed (`household.id!`), regenerated protocol/client output, added migration `20260912110426389-household-id-int` (`text` → `bigint`; Serverpod recreates the two member tables, wiping demo data by design — consistent with #14), re-snapshotted `.generated_manifest.json`, and updated the app converter (`member.householdId.toString()`; the app domain keeps `String` ids). Dev + test DBs migrated; server tests and app tests green. | engineering follow-up | completed 2026-09-12 |
| 13 | `gen_random_uuid_v7()` in `apps/server/migrations/*/definition.sql` was initially flagged as unused. Assessment: the function IS actively used — it is the `DEFAULT` for `uuid PRIMARY KEY` columns on all 19 serverpod_auth_core and serverpod_auth_idp tables (RefreshToken, UserProfile, ServerSideSession, AuthUser, AnonymousAccount, etc.). It is a legitimate customisation (UUID v7 for time-sortable auth identifiers) captured by `serverpod create-migration`. Removing it would break existing auth schema defaults. No action needed. | investigation complete | completed 2026-09-11 |
| 14 | AEF review finding (household auto-create race + Serverpod 3.4.13 API drift), resolved via a forced migration. Committed sweep 2026-09-12: unique index `householdsOwnerUniqueIndex` on `households.ownerId` (household.yaml), migration `20260911165231716` (reconciles pre-existing enum columnType drift on `household_members.role` / `programs.status` by recreating those tables — kept as-is per user guidance that wiping demo data is a non-cost; the next `create-migration` would demand the same recreation otherwise), `.generated_manifest.json` re-snapshotted (`generate:check` green), and `household_service.dart` rewritten: `findByOwner` gained a `Transaction?` param and `getOrCreateFor` runs in `session.db.transaction<Household>` with bulk `insert(... ignoreConflicts: true)` (singular `insertRow` has no `ignoreConflicts` in 3.4.13) and re-reads the concurrent winner when the conflicting insert is skipped. SQL `DROP TABLE "household_members" CASCADE` / `"programs" CASCADE` deletes demo-only data by design. Covered by DB-backed server tests (household/program-membership/auth/protocol, all green) and pinned-toolchain CI gates (README serverpod_cli 3.4.13, goldens-update.yml generalized matching). | QA | completed 2026-09-12 |
| 15 | Mutation-failure UX (per `loading-states.md` rule 1, never blank loaded content): household add/remove-member and program join/cancel failures previously emitted full `failure` states that replaced the loaded screen. Now the loaded states carry a dismissible `mutationError` (`HouseholdState.loaded` / `ProgramDetailsState.loaded`), the blocs settle failed mutations back onto `loaded(mutationError: failure)`, and `household_screen.dart` / `program_details_screen.dart` render `AppInlineAlert.error` inline (dismiss via `HouseholdMutationErrorDismissed` / `ProgramDetailsMutationErrorDismissed`). Initial-load failures keep the full failure branch + Retry. Covered by bloc tests (add/rem / join/cancel failure-keeps-loaded, dismiss) and widget tests (failed join shows inline alert over loaded content; alert dismissible). `loading-states.md` updated to document the settled-mutation-failure row. | QA | completed 2026-09-12 |
| 16 | Mobile bottom navigation gap (narrow-screen shell shows a collapsed `AppNavigationRail`, no bottom bar): deliberately no Material `NavigationBar` fallback (token/tap-target divergence + no-invented-Material rule). Documented as UPSTREAM_UI_GAP-016 and reported as shipitinc/shipit-ui#15 (2026-09-12). Resolved 2026-09-12: upstream shipped the `AppBottomNavigationBar` component (`shipit_ui@c310a961aa`, closes shipitinc/shipit-ui#15); Golden App bumped the pin and `app_shell.dart` now uses `AppNavigationRail` on tablet/desktop and `AppBottomNavigationBar` on compact/mobile widths. Covered by a shell widget test (compact width swaps the rail for the bottom bar; tab switching through `nav-household` / `nav-programs` keys). | shipit-ui upstream | resolved 2026-09-12 |